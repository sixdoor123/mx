<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/bg_fw"
    android:paddingLeft="@dimen/margin_left"
    android:paddingRight="@dimen/margin_right"
    android:paddingTop="@dimen/margin_top_10"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/tv_mall_detail"
        style="@style/bg_ww"
        android:text="正在加载产品详情..."
        android:textColor="@color/bg_hui1"
        android:textSize="@dimen/text_size_15" />

    <ImageView
        android:id="@+id/img_mall_detail"
        android:layout_marginTop="10dp"
        style="@style/img_mall_detail_project" />

    <View
        style="@style/view_line_2px"
        android:layout_marginTop="15dp" />

</LinearLayout>