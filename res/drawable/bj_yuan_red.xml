<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape= "oval"
    android:useLevel= "false">

    <!-- 填充的颜色 -->
    <solid android:color="@color/day_text_red" />

    <!-- 边框的颜色和粗细 -->
    <stroke
        android:width="1dp"
        android:color="@color/day_text_red" />

    <size
        android:width="10dp"
        android:height="10dp" />

</shape>